{
  "name": "symphony",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "posttest": "npm run report:merge && npm run report:marge",
    "report:merge": "mochawesome-merge cypress/reports/mocha/*.json > mochawesome.json",
    "report:marge": "marge mochawesome.json -f report -o cypress/reports"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.13.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
